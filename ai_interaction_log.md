### Task: API Setup
**Prompt:** This prompt was rather long, but I just asked codex to help me set up my API using the Hugging Face Inference Browser. I asked for it to guide me in setting up the endpoint, inputting the model (which we also had experience doing earlier), and making sure that the Hugging Face Token is not hardcoded or mentioned explicitily anywhere in app.py, but rather uses the hidden toml file to read the token. Also to help implement a failsafe to avoid any crashes if there is no token present in the appropriate file, and output an error message stating what the issue is. This secret token should be under the streamlit file under the variable HF_TOKEN, which can be accessed using 'st.secrets["HF_TOKEN"]', which is then stored in another variable (hf_token) in app.py for easy readability. Also, there should be a formatted file in this folder as well that addresses the theme/setup of the display. I also fed it the example request structure for reference so that it can better integrate it into the code. 
**AI Suggestion:** AI recommended storing the endpoint and the model as hard variables. It also helped me make sure my token was being stored appropriately and that it wouldn't hard code any token, but read it through streamlits module as it is stored in the secret file. It took the recommendation for an example request structure, and said it can be integrated, but it wanted to use the hard variables with the information stored, rather than keeping it directly on there. It also recommended making my repository public.
**My Modifications & Reflections:** I took AI's recommendations because I figured it would be better to hard code (NOT the token) but some of the links like the endpoint, model, etc., that way its easy to change is necessary and you dont have to sort through a bunch of code. I also added my secrets file to gitignore along with my venv file, so that my token is not exposed, which was recommended by the assignment instructions, not AI. I took its advice and made my repository public because it was not connecting between streamlit and github when private (the repository was not showing up).

### Task: 1 (Part A)
**Prompt:** I need help now with the page setup and API connection using the streamlit module, particularly st.set_page_configuration with the header "My AI Chat", in a specific format. Then I need to st.secrets["HF_TOKEN"] to read the token stored in the secrets.toml file (the token should NOT be hard coded or exposed in app.py). Then help me build a fail safe, where if the token is not present (appropriately) or missing entirely, an error message will be output stating exactly what the issue is. Then there needs to be a single hard coded message ("Hello!") sent to the Hugging Face API using the token loaded to hf_token (from the secret file - REMEMBER do NOT expose), with the models response being displayed in the main area. Finally, the program should also handle API errors in an effective manner, displaying an error message for missing token, invalid token, rate limit, network failure, rather than crashing. Streamlit should be able to run with a valid token, send the test message, and display the model response. If the program were to run without the token, it would display an error message rather than crashing. 
**AI Suggestion:** AI recommended beginning with a token validation first using st.secrets["HF_TOKEN"] along with an empty check (if the token were not present). Then it recommended using an early stop to catch, particularly using st.error() and st.stop(), to prevent any potential crashing when the secret token is missing or not in appropriate format. It then recommended keeping the API request minimal, using a Hello request, fixed model, or 'max_tokens', to confirm connectivity before the chat flow is built fully. Then it suggested, as a way to handle other API errors, to handle non-200 responses by displaying a clear error message as to what status code is showing up and to provide some quick context to potentially help the user troubleshoot. Next, it said to use a try/except and wrap the request (particularly using 'requests.exceptions.RequestException') to address any issues around DNS or network or timeout, in a way that is clean and helpful to the user. 
**My Modifications & Reflections:** I followed the AI recommendation, as I explicitly mentioned for it to make sure my token is not exposed, so I did not want to take any chances and expose it. It said sometimes the token can be exposed because it exists as a plain text inside the secrets toml file, but .gitignore should help with that. It also successfully helped me set up the page using streamlit and test the API, handling any errors with a clean and helpful message. 

### Task: 1 (Part B)
**Prompt:** Please help me implement a multi-turn conversation UI (User Interface), where the hard coded message being sent to the Hugging Face API, is replaced instead by a real input interface. Next, help me use the .streamlit native module to access its UI chat elements to render messages using 'st.chat_message()' and 'st.chat_input()' to collect the input from the user. At the bottom of the main area, help me put a fixed input bar to collect the users input. Then the entire conversation needs to be stored using 'st.session_state', with each exchange, both the user and the generated assistant response is logged in the conversation history. This conversation history then needs to be sent, along with the API requests, so that the model maintains context for future conversations. The conversation history (above the chat bar) should be using default streamlit elements as opposed to CSS custom chat bubbles. The history between the user and the machine should scroll independently, not attached to the fixed input bar (which should always be visible). By the end, the user should be able to multiple messages and have responses that are aware of the previous context (like names and other info provided by the user).
**AI Suggestion:** AI suggested rendering the message history using native streamlit components geared towards chat/messaging, like 'st.chat_message()' before calling other elements like st.chat_input() to make sure the chat is opened before anything is inputted. Then it recommended using appending (active_chat["messages"]) for both the users input and assistant responses to the session state every turn so that the conversation state remains complete and there are no discrepencies. Then it said to send the full history (according to the assignment) by building request_messages with the info and then sending the whole list, which is stored in a variable. The chat bar can remain anchored using 'st.chat_input()' rather than any custom HTML inputs so that the behavior remainds consistent with streamlits defaults. 
**My Modifications & Reflections:** Because the assignment wanted us to stick to native streamlit defaults, I did not make any modifications to the suggestions it provided. I took its recommendation so that there would be no issues between the user input and assistant output, along with the history being stored and accessed by the model for context. Everything worked well and the information was being retained, say I ask the model something from before (like a name, color, etc.).

### Task: 1 (Part C)
**Prompt:** Now, how could I set up a 'New Chat' button on the side bar that allows for the creation of a new and empty conversation for the user, while adding it to the chats list on the sidebar. To navigate the native streamlit sidebar, help me also incorperate st.sidebar in the code for chat navigation. The sidebar should ideally show a list of all current chats that display a title and timestamp, which the list can be scrolled through. Whatever the current chat is should be highlighted in the sidebar, and clicking another chat on the list should change the chat without messing (deleting or overwriting) the other chats (again highlight whichever chat the user is currently accessing). Next, each chat should have a delete button ('x') on its right side thats allows the user to remove the chat from the list. If the deleted chat was the active one, either the app switches to another chat (if present), if not, it returns to an empty state where it prompts the user to click 'New Chat' to start one. Ideally, multiple chats can be created, swtiched between, and deleted properly, with the active chat always being highlighted for distinction. 
**AI Suggestion:** AI recommended representing each chat as a dictionary with appropriate keys for value storage like id, title, timestamp, messages, etc., which makes switching and accessing information more efficient and straightforward. Then it said to loop through the chats (using st.session_state.chats) and create two buttons using sidebar.columns, with one using the select and switch chats function, while the other right button having the x to delete the chat. Because each chat has a unique id, streamlit should be able to track which chat was accessed (created, deleted, modified) and work accordingly. Using st.session_state.active_chat_id also allows for the click to work and links the id to that chat button. So that if a chat needs to be deleted, it could be identified in the json file and removed, chose a failback (if present) and then use st.rerun. 
**My Modifications & Reflections:** The code did work and there was no overlap or confusion in the information when changing between chats, creating new chats, or deleting old ones. I did have to run codex to look over it one time, but that was mainly for tiny formatting issues, not due to actual performance. I checked to make sure the chat history was removed from the json file when deleted on the UI, which it was. I was also able to start multiple chats and switch between them. 

### Task: 1 (Part D)
**Prompt:** I need help working on chat persistance in my code. Each chat session should be stored as a seperate json file under the chats folder at a minimum chat ID, title/timestamp, and the full message history (like mentioned before it should be titled clearly for easy streamline access). When the app is started, all the exisiting chat history stored in chats folder should be loaded and displayed in the sidebar automatically. Any chats not closed and stored should be accessible and the user should be able to return to them, and be able to continue left off conversations. Again, deleting the chat by pressing the 'x' button should remove it from the json file. This is optional, but it may be nice to have a generated or summarized chat title, it does not have to be a part of the users inital message.
**AI Suggestion:** Ai recommended saving each chat as its own JSON file under the chats folder as 'chats/<chat_id>.json' so that the threads may be easily accessed, as they would remain independent on the list (and reduce the likelyhood of conversation information getting messed up). Also, every time a chat data changes, to immediately save those changes to the file stored on the disk, rather than waiting and if the app closes or refreshes, all the information is still saved. On startup, to make sure all the files and chats are automatically loaded from the disk, check st.session_state["chats"], if it does not exist then load the chats from the disk using a function that accesses it so. The function should scan the chats folder and then return all stored files as a list and then that list is stored in that session state but as st.session_states.chats and the first chat is made active. If no files exist, then a new default one is created. If there is an issue with reading the files under chat, skip opening it with an error message rather than crashing. 
**My Modifications & Reflections:** The code suggestion worked and was able to store information properly, so that next time the page was loaded, all the chat information was also automatically loaded and displayed. There was a tiny issue with some of the information being displayed, but I used codex to troubleshoot it and it patched it accordingly (there were some square shapes showing up rather than letters). I looked again, to make sure there were no errors or bugs in the information stored, but the few times I reran the program, everything seemed to hold up pretty well. 

### Task: 2
**Prompt:** I need help with this task that works to display the models reply token by token as it is generated instead of waiting for the full response. Help me integrate the stream=True parameter in my API request to handle the server-sent event stream. Also, shoukd I use the native streamlit methods like st.write_stream(), or use a manual update as a placeholder using st.empty() as the chunks of information come in? The full streamed response should be saved to the chat history following the completion of the streaming, what is the best way to approach that? Also, there is a hint provided that says "Add stream=True to your request payload and set stream=True on the requests.post() call. The response body will be a series of data: lines in SSE format." Depending on how the response runs, I may also need to implement a very short delay between the chunks due to the llama model being used. 
**AI Suggestion:** AI suggested to set stream=True in both place matters (request payload and 'requests.post(..., stream=True)') as they serve different roles, so that a streamed response format can be generated and requests.post for reading chunks as they arrive rather than waiting. Then it said to parse only data lines (as the SSE responses come as many lines), as they are the only ones that contain model events, by using line.startswith("data:"), strip the prefix, and parse that particular JSON chunk. Also be sure to ignore any malformed or empty lines cleanly using continue, and break loop when DONE is reached (end of payload). It suggested using the st.empty() placeholder to overwrite repeatedly to see the text appear in a "typing" effect. Keep a growing assistant_text string and on each chunk, append the new text and use placeholder.markdown(assistant_text) to keep going. Using a time.sleep(0.01) can help achieve that slight delay for visual clariry with faster models like llama. 
**My Modifications & Reflections:** I took the st.empty() recommendation and utilized it to achieve the desired effect (displaying quickly in chunks) as wellas the time.sleep() which helped with visual clarity which was good. As for the recommendation of putting stream=True in both areas, it helped to also read the chunks so that the formatting is proper. Also I took the recommendations for reading only the lines that start with 'data' as to avoid any issues in the information being returned. 

### Task: 3
**Prompt:** Could you help me now to personalize the model a bit. I basically want to extract and store user preferences from user input, so responses can become more personalized. After every assistant response, another small API call needs to be made that extracts personal traits or preferences that are entered in the users input. That information then needs to be stored in a seperate json file (memory.json), with catagories like name, language preference, communication style, favorite topics, or other useful personal info. The sidebar then displays 'User Memory' (expander panel) that displays all the recorded personal information. I need to the use a streamlit native control that can clear the saved memory from display and the json file as well. The stored information (personal) is used in the system prompts (responses) for any later conversations so that responses can be more personalized. Here is a hint given in my assignment: “Given this user message, extract any personal facts or preferences as a JSON object. If none, return {}”.
**AI Suggestion:** AI suggested using a short prompt like "extract traits as json and if not, then return {}". This is to help it remain seperate from call used for the assistant-reply, so there is no mix up or confusion. Again if it fails or no traits are currently provided, then {} is returned and the program does not do anything until the next input. Merging the information into the json file is goin to require a delicate process. So for the scalar fields (like name) the overwrite only occurs when the new value is valid, interests need to make sure no duplicate values are being stored (append and then deduplicate), and to retain the old data should the new information be empty or unclear/ambiguious. As for the User Memory section in the sidebar, use the same formatting used before for other buttons but connect to memory.json file, and this time use an expander so that the display is collapsable and interactive. Also provide a clear memory button that deletes the information from the json file. Again the information stored will be placed into the memory for future prompts and this should be done before each chat completion call, where the system message is added containing memory.json. 
**My Modifications & Reflections:** The code did work overall, I just had to do some trouble shooting using codex for some formatting variances and make some decisions about what personal catagories I want to include. I just opted for the ones provided in the assignment for simplicities sake. I went with codex's recommendation for using merge_memory and save_memory as a way to merge and store the memory, and memory.json and json.dump for json file storage. Then using load_memory() I was able to call to the information (if any) stored in the json file, parse the data (dictionary), and return it. If there is no information (or any errors), an empty dictionary is returned. 